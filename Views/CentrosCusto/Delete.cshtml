@model SistemaTesourariaEclesiastica.Models.CentroCusto

@{
    ViewData["Title"] = "Excluir Centro de Custo";
}

<div class="d-flex justify-content-between align-items-center mb-4">
    <h2><i class="fas fa-trash me-2 text-danger"></i>Excluir Centro de Custo</h2>
    <a asp-action="Index" class="btn btn-secondary">
        <i class="fas fa-arrow-left me-2"></i>Voltar
    </a>
</div>

<div class="alert alert-warning" role="alert">
    <i class="fas fa-exclamation-triangle me-2"></i>
    <strong>Atenção!</strong> Você está prestes a excluir este centro de custo. Esta ação não pode ser desfeita.
</div>

<div class="card">
    <div class="card-body">
        <h5 class="card-title">Confirmar Exclusão</h5>
        <p class="card-text">Tem certeza de que deseja excluir este centro de custo?</p>
        
        <dl class="row">
            <dt class="col-sm-3">@Html.DisplayNameFor(model => model.Nome)</dt>
            <dd class="col-sm-9">@Html.DisplayFor(model => model.Nome)</dd>

            <dt class="col-sm-3">@Html.DisplayNameFor(model => model.Tipo)</dt>
            <dd class="col-sm-9">
                <span class="badge bg-info">
                    @Model.Tipo.GetDisplayName()
                </span>
            </dd>

            <dt class="col-sm-3">@Html.DisplayNameFor(model => model.Descricao)</dt>
            <dd class="col-sm-9">@Html.DisplayFor(model => model.Descricao)</dd>

            <dt class="col-sm-3">@Html.DisplayNameFor(model => model.Ativo)</dt>
            <dd class="col-sm-9">
                @if (Model.Ativo)
                {
                    <span class="badge bg-success">Ativo</span>
                }
                else
                {
                    <span class="badge bg-secondary">Inativo</span>
                }
            </dd>
        </dl>

        <form asp-action="Delete" class="d-inline">
            <input type="hidden" asp-for="Id" />
            <div class="d-grid gap-2 d-md-flex justify-content-md-end">
                <a asp-action="Index" class="btn btn-secondary me-md-2">Cancelar</a>
                <button type="submit" class="btn btn-danger">
                    <i class="fas fa-trash me-2"></i>Confirmar Exclusão
                </button>
            </div>
        </form>
    </div>
</div>

<div class="alert alert-info mt-3" role="alert">
    <i class="fas fa-info-circle me-2"></i>
    <strong>Nota:</strong> Se este centro de custo possuir movimentações financeiras associadas, 
    a exclusão pode não ser permitida para manter a integridade dos dados.
</div>
