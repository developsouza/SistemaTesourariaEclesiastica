@model SistemaTesourariaEclesiastica.Models.CentroCusto
@using SistemaTesourariaEclesiastica.Enums

@{
    ViewData["Title"] = "Nova Unidade Financeira";
}

<div class="d-flex justify-content-between align-items-center mb-4">
    <div>
        <h1 class="h3 mb-0">
            <i class="bi bi-plus-circle me-2"></i>
            Nova Unidade Financeira
        </h1>
        <p class="text-muted mb-0">Cadastrar nova unidade financeira ou congregação</p>
    </div>
    <a asp-action="Index" class="btn btn-outline-secondary">
        <i class="bi bi-arrow-left me-2"></i>Voltar
    </a>
</div>

<div class="row">
    <div class="col-lg-8">
        <div class="card shadow-sm">
            <div class="card-header">
                <h5 class="mb-0">
                    <i class="bi bi-info-circle me-2"></i>
                    Informações da Unidade Financeira
                </h5>
            </div>
            <div class="card-body">
                <form asp-action="Create">
                    <div asp-validation-summary="ModelOnly" class="alert alert-danger" role="alert"></div>

                    <div class="row">
                        <div class="col-md-6 mb-3">
                            <label asp-for="Nome" class="form-label">Nome <span class="text-danger">*</span></label>
                            <input asp-for="Nome" class="form-control" placeholder="Ex: Sede Principal" />
                            <span asp-validation-for="Nome" class="text-danger"></span>
                        </div>

                        <div class="col-md-6 mb-3">
                            <label asp-for="Tipo" class="form-label">Tipo <span class="text-danger">*</span></label>
                            <select asp-for="Tipo" class="form-select" asp-items="Html.GetEnumSelectList<TipoCentroCusto>()"
                                    required>
                                <option value="">Selecione o tipo</option>
                            </select>
                            <span asp-validation-for="Tipo" class="text-danger"></span>
                            <div class="invalid-feedback">
                                Por favor, selecione um tipo para a unidade financeira.
                            </div>
                        </div>
                    </div>

                    <div class="mb-3">
                        <label asp-for="Descricao" class="form-label">Descrição</label>
                        <textarea asp-for="Descricao" class="form-control" rows="3"
                                  placeholder="Descreva o propósito desta unidade financeira..."></textarea>
                        <span asp-validation-for="Descricao" class="text-danger"></span>
                        <div class="form-text">Máximo de 250 caracteres</div>
                    </div>

                    <div class="mb-4">
                        <div class="form-check form-switch">
                            <input asp-for="Ativo" class="form-check-input" type="checkbox" checked />
                            <label asp-for="Ativo" class="form-check-label">Unidade financeira ativa</label>
                        </div>
                        <div class="form-text">Desative se esta unidade financeira não estiver em uso no momento</div>
                    </div>

                    <hr class="my-4">

                    <div class="d-flex gap-2 justify-content-end">
                        <a asp-action="Index" class="btn btn-outline-secondary">
                            <i class="bi bi-x-circle me-2"></i>Cancelar
                        </a>
                        <button type="submit" class="btn btn-primary">
                            <i class="bi bi-check-circle me-2"></i>Cadastrar
                        </button>
                    </div>
                </form>
            </div>
        </div>
    </div>

    <div class="col-lg-4">
        <div class="card shadow-sm border-primary border-opacity-25">
            <div class="card-header bg-primary bg-opacity-10">
                <h6 class="mb-0 text-primary">
                    <i class="bi bi-lightbulb me-2"></i>Dicas e Orientações
                </h6>
            </div>
            <div class="card-body">
                <p class="small text-muted mb-3">
                    As unidades financeiras são utilizadas para organizar e categorizar as receitas e
                    despesas da igreja, permitindo um melhor controle financeiro e geração de relatórios específicos.
                </p>

                <hr class="my-3">

                <h6 class="small fw-bold mb-2">Tipos de Unidade Financeira:</h6>
                <ul class="small text-muted ps-3">
                    <li class="mb-1"><strong>Sede:</strong> Igreja matriz</li>
                    <li class="mb-1"><strong>Congregacao:</strong> Congregações locais</li>
                    <li class="mb-1"><strong>Departamento:</strong> Departamentos específicos</li>
                    <li class="mb-1"><strong>Projeto:</strong> Projetos temporários</li>
                    <li class="mb-1"><strong>Ministerio:</strong> Ministérios da igreja</li>
                </ul>

                <hr class="my-3">

                <div class="alert alert-info small mb-0">
                    <i class="bi bi-info-circle me-1"></i>
                    <strong>Importante:</strong> O nome da unidade financeira deve ser único no sistema.
                </div>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}