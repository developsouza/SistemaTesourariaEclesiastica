@model IEnumerable<dynamic>

@{
    ViewData["Title"] = "Balancete Geral";
}

<!-- Cabeçalho -->
<div class="d-flex flex-column flex-md-row justify-content-between align-items-start align-items-md-center mb-4 gap-2">
    <div>
        <h1 class="h3 mb-1">
            <i class="bi bi-calculator me-2 text-primary"></i>Balancete Geral
        </h1>
        <p class="text-muted mb-0 small">Visão consolidada por categoria contábil</p>
    </div>
    <div class="d-flex gap-2">
        <button type="button" class="btn btn-danger btn-sm" onclick="window.print()">
            <i class="bi bi-file-pdf me-1"></i>PDF
        </button>
        <a asp-action="Index" class="btn btn-outline-secondary btn-sm">
            <i class="bi bi-arrow-left me-1"></i>Voltar
        </a>
    </div>
</div>

<!-- Filtros -->
<div class="card shadow-sm mb-4">
    <div class="card-body">
        <form method="get" class="row g-3">
            <div class="col-12 col-md-5">
                <label class="form-label small fw-semibold">Data Início</label>
                <input type="date" class="form-control" name="dataInicio" value="@ViewBag.DataInicio" />
            </div>
            <div class="col-12 col-md-5">
                <label class="form-label small fw-semibold">Data Fim</label>
                <input type="date" class="form-control" name="dataFim" value="@ViewBag.DataFim" />
            </div>
            <div class="col-12 col-md-2 d-flex align-items-end">
                <button type="submit" class="btn btn-primary w-100">
                    <i class="bi bi-search me-1"></i>Filtrar
                </button>
            </div>
        </form>
    </div>
</div>

@{
    decimal totalEntradas = 0;
    decimal totalSaidas = 0;
    decimal saldoGeral = 0;

    if (Model != null && Model.Any())
    {
        foreach (var item in Model)
        {
            totalEntradas += item.TotalEntradas;
            totalSaidas += item.TotalSaidas;
            saldoGeral += item.Saldo;
        }
    }
}

@if (Model != null && Model.Any())
{
    <!-- Cards de Resumo -->

    <div class="row g-3 mb-4">
        <div class="col-12 col-md-4">
            <div class="card shadow-sm border-0 bg-success bg-opacity-10 border-start border-success border-4">
                <div class="card-body">
                    <div class="d-flex align-items-center">
                        <div class="flex-grow-1">
                            <small class="text-muted d-block">Total de Entradas</small>
                            <h4 class="mb-0 fw-bold text-success">@totalEntradas</h4>
                        </div>
                        <div class="flex-shrink-0">
                            <i class="bi bi-arrow-down-circle text-success" style="font-size: 2rem;"></i>
                        </div>
                    </div>
                </div>
            </div>
        </div>
        <div class="col-12 col-md-4">
            <div class="card shadow-sm border-0 bg-danger bg-opacity-10 border-start border-danger border-4">
                <div class="card-body">
                    <div class="d-flex align-items-center">
                        <div class="flex-grow-1">
                            <small class="text-muted d-block">Total de Saídas</small>
                            <h4 class="mb-0 fw-bold text-danger">@totalSaidas</h4>
                        </div>
                        <div class="flex-shrink-0">
                            <i class="bi bi-arrow-up-circle text-danger" style="font-size: 2rem;"></i>
                        </div>
                    </div>
                </div>
            </div>
        </div>
        <div class="col-12 col-md-4">
            <div class="card shadow-sm border-0 bg-primary bg-opacity-10 border-start border-primary border-4">
                <div class="card-body">
                    <div class="d-flex align-items-center">
                        <div class="flex-grow-1">
                            <small class="text-muted d-block">Saldo do Período</small>
                            <h4 class="mb-0 fw-bold @(saldoGeral >= 0 ? "text-primary" : "text-danger")">
                                @saldoGeral
                            </h4>
                        </div>
                        <div class="flex-shrink-0">
                            <i class="bi bi-wallet2 @(saldoGeral >= 0 ? "text-primary" : "text-danger")" style="font-size: 2rem;"></i>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <!-- Tabela de Balancete -->
    <div class="card shadow-sm">
        <div class="card-header bg-white d-flex justify-content-between align-items-center py-3">
            <div>
                <h5 class="mb-1 fw-semibold">
                    <i class="bi bi-table me-2 text-primary"></i>
                    Demonstrativo por Categoria
                </h5>
                <small class="text-muted">
                    Período: @DateTime.Parse(ViewBag.DataInicio).ToString("dd/MM/yyyy") a @DateTime.Parse(ViewBag.DataFim).ToString("dd/MM/yyyy")
                </small>
            </div>
            <button type="button" class="btn btn-sm btn-outline-secondary d-none d-md-inline-block" onclick="window.print()">
                <i class="bi bi-printer me-1"></i>Imprimir
            </button>
            <button type="button" class="btn btn-sm btn-outline-secondary d-md-none" onclick="window.print()">
                <i class="bi bi-printer"></i>
            </button>
        </div>
        <div class="card-body p-0">
            <!-- Versão Desktop (Tabela) -->
            <div class="table-responsive d-none d-lg-block">
                <table class="table table-hover align-middle mb-0" id="balanceteTable">
                    <thead class="table-light">
                        <tr>
                            <th class="ps-3">Plano de Contas</th>
                            <th class="text-center">Tipo</th>
                            <th class="text-end">Entradas</th>
                            <th class="text-end">Saídas</th>
                            <th class="text-end pe-3">Saldo</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var item in Model)
                        {
                            <tr>
                                <td class="ps-3">
                                    <div class="fw-semibold">@item.PlanoContasNome</div>
                                </td>
                                <td class="text-center">
                                    @if (item.Tipo.ToString() == "Receita")
                                    {
                                        <span class="badge bg-success">
                                            <i class="bi bi-arrow-down me-1"></i>Receita
                                        </span>
                                    }
                                    else
                                    {
                                        <span class="badge bg-danger">
                                            <i class="bi bi-arrow-up me-1"></i>Despesa
                                        </span>
                                    }
                                </td>
                                <td class="text-end">
                                    <span class="text-success fw-semibold">
                                        @((decimal)item.TotalEntradas)
                                    </span>
                                </td>
                                <td class="text-end">
                                    <span class="text-danger fw-semibold">
                                        @((decimal)item.TotalSaidas)
                                    </span>
                                </td>
                                <td class="text-end pe-3">
                                    <span class="fw-bold @(item.Saldo >= 0 ? "text-success" : "text-danger")">
                                        @((decimal)item.Saldo)
                                    </span>
                                </td>
                            </tr>
                        }
                    </tbody>
                    <tfoot class="table-light">
                        <tr>
                            <th class="ps-3" colspan="2">TOTAIS</th>
                            <th class="text-end">
                                <span class="fw-bold text-success">@totalEntradas</span>
                            </th>
                            <th class="text-end">
                                <span class="fw-bold text-danger">@totalSaidas</span>
                            </th>
                            <th class="text-end pe-3">
                                <span class="fw-bold @(saldoGeral >= 0 ? "text-success" : "text-danger")">
                                    @saldoGeral
                                </span>
                            </th>
                        </tr>
                    </tfoot>
                </table>
            </div>

            <!-- Versão Mobile (Cards) -->
            <div class="d-lg-none">
                @foreach (var item in Model)
                {
                    <div class="border-bottom p-3">
                        <div class="d-flex justify-content-between align-items-start mb-2">
                            <div class="flex-grow-1">
                                <div class="fw-bold mb-1">@item.PlanoContasNome</div>
                                @if (item.Tipo.ToString() == "Receita")
                                {
                                    <span class="badge bg-success">
                                        <i class="bi bi-arrow-down me-1"></i>Receita
                                    </span>
                                }
                                else
                                {
                                    <span class="badge bg-danger">
                                        <i class="bi bi-arrow-up me-1"></i>Despesa
                                    </span>
                                }
                            </div>
                        </div>
                        
                        <div class="row g-2 mt-2">
                            <div class="col-4">
                                <small class="text-muted d-block">Entradas</small>
                                <div class="fw-semibold text-success">@((decimal)item.TotalEntradas)</div>
                            </div>
                            <div class="col-4">
                                <small class="text-muted d-block">Saídas</small>
                                <div class="fw-semibold text-danger">@((decimal)item.TotalSaidas)</div>
                            </div>
                            <div class="col-4">
                                <small class="text-muted d-block">Saldo</small>
                                <div class="fw-bold @(item.Saldo >= 0 ? "text-success" : "text-danger")">
                                    @((decimal)item.Saldo)
                                </div>
                            </div>
                        </div>
                    </div>
                }
                
                <!-- Total para Mobile -->
                <div class="bg-light p-3">
                    <div class="fw-bold mb-2">TOTAIS</div>
                    <div class="row g-2">
                        <div class="col-4">
                            <small class="text-muted d-block">Entradas</small>
                            <div class="fw-bold text-success fs-5">@totalEntradas</div>
                        </div>
                        <div class="col-4">
                            <small class="text-muted d-block">Saídas</small>
                            <div class="fw-bold text-danger fs-5">@totalSaidas</div>
                        </div>
                        <div class="col-4">
                            <small class="text-muted d-block">Saldo</small>
                            <div class="fw-bold @(saldoGeral >= 0 ? "text-success" : "text-danger") fs-5">
                                @saldoGeral
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
}
else
{
    <div class="card shadow-sm">
        <div class="card-body text-center py-5">
            <i class="bi bi-inbox text-muted" style="font-size: 4rem;"></i>
            <h5 class="mt-3 text-muted">Nenhum dado encontrado</h5>
            <p class="text-muted">Não há movimentações para o período selecionado.</p>
        </div>
    </div>
}

@section Styles {
    <style>
        @@media print {
            .btn, .card-header .btn-group {
                display: none !important;
            }

            .card {
                border: 1px solid #000;
                box-shadow: none !important;
            }
        }
    </style>
}